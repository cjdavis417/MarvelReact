import React from 'react';
import Backdrop from '@material-ui/core/Backdrop';

const Splash = () => {
    return (
        <div>
            <Backdrop></Backdrop>
            Splash</div>

    );
};

export default Splash;